# Direkte Abhängigkeiten des Templates (Python >= 3.11).
# Transitive Pakete werden von pip automatisch aufgelöst.
pandas==3.0.3
numpy==2.5.1
requests==2.34.2
python-dotenv==1.2.2
altair==6.2.2

# Notebooks (Kernel + Ausgabe-Strip beim Commit)
ipykernel==7.3.0
ipywidgets==8.1.8
nbstripout==0.9.1
